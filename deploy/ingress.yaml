# LiveKit Ingress config — TEMPLATE (RTMP / WHIP / URL-pull).
# Se pasa al contenedor via -e INGRESS_CONFIG_BODY="$(cat ingress.yaml)".
redis:
  address: localhost:6379
ws_url: ws://localhost:7880
api_key: <API_KEY>
api_secret: <API_SECRET>
rtmp_port: 1935
whip_port: 8080
http_relay_port: 9090
logging:
  level: info
